name: Frontend CI

on:
  push:
    branches: [develop, main]
    paths:
      - "frontend/**"
  pull_request:
    branches: [develop, main]
    paths:
      - "frontend/**"

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
